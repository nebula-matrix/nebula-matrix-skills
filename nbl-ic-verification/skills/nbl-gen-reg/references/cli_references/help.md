# help

显示 gen_reg_tools 的完整使用指南，包含所有命令、参数和工作流说明。

## 参数

无。

## 示例

```bash
gen-reg help
```

## 输出内容

- 所有支持的命令列表
- 每个命令的详细参数说明
- 典型工作流示例
- 获取子命令帮助的方法

## 替代方式

每个子命令也有独立的帮助信息：

```bash
gen-reg extract --help
gen-reg regslv --help
gen-reg ral --help
```
